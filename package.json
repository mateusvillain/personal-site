{
  "name": "personal-site",
  "version": "2.7.1",
  "description": "",
  "main": "dist/index.html",
  "scripts": {
    "build": "npm install --prefix blog && cross-env NODE_ENV=production node build.js && npm run build:css:general && npm run build:css:blog && npm run build:blog && npm run build:img && npm run build:sitemap && npm run build:robots",
    "build:css:general": "sass src/sass/general.scss dist/style.css --style=compressed --no-source-map",
    "build:css:blog": "sass src/sass/blog.scss dist/blog.css --style=compressed --no-source-map",
    "build:sitemap": "node sitemap.js",
    "build:robots": "node robots.js",
    "build:blog": "npm --prefix blog run build",
    "build:img": "node src/js/images.js",
    "watch:html": "cross-env NODE_ENV=development nodemon --watch src --ext html --exec \"node build.js\"",
    "watch:css": "sass src/sass/general.scss dist/style.css --watch",
    "serve": "browser-sync start --server \"dist\" --files \"dist/**/*\" --open",
    "dev:front": "npm-run-all --parallel watch:html watch:css serve",
    "release:major": "npm version major && git push origin main --tags",
    "release:minor": "npm version minor && git push origin main --tags",
    "release:patch": "npm version patch && git push origin main --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mateusvillain/personal-site.git"
  },
  "keywords": [
    "personal",
    "site",
    "portfolio",
    "design system"
  ],
  "author": "Mateus Villain",
  "license": "MIT",
  "type": "module",
  "bugs": {
    "url": "https://github.com/mateusvillain/personal-site/issues"
  },
  "homepage": "https://github.com/mateusvillain/personal-site#readme",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "cross-env": "^10.1.0",
    "esbuild": "^0.27.2",
    "nodemon": "^3.1.11",
    "npm-run-all": "^4.1.5",
    "posthtml": "^0.16.7",
    "posthtml-include": "^2.0.1",
    "prettier": "^3.8.1",
    "sass": "^1.94.2",
    "vercel": "^50.8.1"
  },
  "dependencies": {
    "@notionhq/client": "^5.6.0",
    "dotenv": "^17.2.3",
    "glob": "^13.0.3",
    "sharp": "^0.34.5"
  }
}
