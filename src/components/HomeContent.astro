<div class="body-container">
  <lui-stack gap="80">
    <div class="desktop">
      <div class="personal-info">
        <lui-body variant="sm" id="hora"></lui-body>
        <lui-body variant="sm" id="clima"></lui-body>
        <lui-body variant="sm" id="spotify"></lui-body>
      </div>
      <div class="site-controls">
        <a
          class="body--sm link"
          id="lang-switch"
          data-i18n="language"
          data-link="language"></a>
        <label class="theme-toggle" aria-label="Alternar tema">
          <input type="checkbox" id="theme-toggle" />
          <div class="theme-toggle__track">
            <div class="theme-toggle__thumb"></div>
            <div class="theme-toggle__icon theme-toggle__icon--sun">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                fill="none"
                viewBox="0 0 16 16"
              >
                <g clip-path="url(#a)">
                  <path
                    fill="var(--lui-color-secondary-icon-default)"
                    d="M7.5 2.5V1a.5.5 0 0 1 1 0v1.5a.5.5 0 1 1-1 0ZM12 8a4 4 0 1 1-8 0 4 4 0 0 1 8 0Zm-1 0a3 3 0 1 0-6 0 3 3 0 0 0 6 0ZM3.646 4.354a.5.5 0 1 0 .708-.708l-1-1a.5.5 0 1 0-.708.708l1 1Zm0 7.292-1 1a.5.5 0 0 0 .708.708l1-1a.501.501 0 0 0-.708-.708ZM12 4.5a.5.5 0 0 0 .354-.146l1-1a.5.5 0 0 0-.708-.708l-1 1A.5.5 0 0 0 12 4.5Zm.354 7.146a.5.5 0 0 0-.708.708l1 1a.501.501 0 0 0 .708-.708l-1-1ZM3 8a.5.5 0 0 0-.5-.5H1a.5.5 0 0 0 0 1h1.5A.5.5 0 0 0 3 8Zm5 5a.5.5 0 0 0-.5.5V15a.5.5 0 0 0 1 0v-1.5A.5.5 0 0 0 8 13Zm7-5.5h-1.5a.5.5 0 0 0 0 1H15a.5.5 0 0 0 0-1Z"
                  ></path>
                </g>
                <defs>
                  <clipPath id="a">
                    <path fill="#fff" d="M0 0h16v16H0z"></path>
                  </clipPath>
                </defs>
              </svg>
            </div>
            <div class="theme-toggle__icon theme-toggle__icon--moon">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="16"
                height="16"
                fill="none"
                viewBox="0 0 16 16"
              >
                <path
                  fill="var(--lui-color-secondary-icon-default)"
                  d="M14.596 8.89a.5.5 0 0 0-.5-.126 5.505 5.505 0 0 1-6.862-6.862.5.5 0 0 0-.625-.625A6.5 6.5 0 0 0 8.5 14a6.443 6.443 0 0 0 3.908-1.305 6.553 6.553 0 0 0 2.312-3.307.5.5 0 0 0-.124-.499Zm-2.79 3.006a5.5 5.5 0 1 1-5.74-9.327 6.507 6.507 0 0 0 7.366 7.364 5.562 5.562 0 0 1-1.626 1.963Z"
                ></path>
              </svg>
            </div>
          </div>
        </label>
      </div>
    </div>

    <main class="hero">
      <lui-flex direction="column" wrap="nowrap" gap="16" align="left">
        <lui-image
          src="img/mtsvlln.jpg"
          width="88px"
          aspect-ratio="1/1"
          radius="md"
          alt="Fotografia de Mateus Villain: Homem de barba cheia e cabeça raspada, usando óculos de armação arredondada, olha diretamente para a câmera. Ele segura um copo de café com tampa. A foto tem estilo de polaroid, com fundo desfocado em ambiente interno."
        ></lui-image>

        <div>
          <lui-heading variant="block-title" as="h1">Mateus Villain</lui-heading
          >
          <lui-body variant="lg" data-i18n="role" italic></lui-body>
        </div>

        <lui-body variant="lg" data-i18n="short-bio"></lui-body>
      </lui-flex>

      <ul class="no-list links">
        <li>
          <lui-link href="/blog/" data-link="blog">Blog</lui-link>
        </li>
        <li>
          <lui-link href="/mentorship/" data-i18n="menu-mentorship"></lui-link>
        </li>
        <li>
          <lui-link
            href="https://linkedin.com/in/mateusvillain/"
            target="_blank"
            data-link="linkedin"
            data-social="linkedin"
            external
          >
            LinkedIn
          </lui-link>
        </li>
        <li>
          <lui-link
            href="https://github.com/mateusvillain/"
            target="_blank"
            data-link="github"
            data-social="github"
            external
          >
            GitHub
          </lui-link>
          <a
            rel="me"
            href="https://mastodon.social/@mateusvillain"
            style="display: none;">Mastodon</a
          >
        </li>
      </ul>
    </main>

    <section class="board" aria-labelledby="projects-title">
      <div class="board__header">
        <lui-heading variant="block-title" as="h2" id="projects-title" data-i18n="projects"
        ></lui-heading>
        <button
          type="button"
          class="board__reset"
          id="widgets-reset"
          data-i18n="projects-reset"
          hidden></button>
      </div>

      <div class="board__stage" id="widget-board">
        <article
          class="widget widget--tokens"
          data-widget="locaweb"
          tabindex="0"
          aria-roledescription="widget"
          aria-labelledby="widget-locaweb-title"
        >

          <div class="widget__body">
            <lui-body
              variant="lg"
              as="h3"
              class="widget__title"
              id="widget-locaweb-title"
            >
              <a
                href="/case/locaweb-design-system/"
                data-link="locaweb-design-system"
                data-i18n="locaweb-title"></a>
            </lui-body>
            <lui-body variant="md" data-i18n="locaweb-description"></lui-body>

            <div class="widget__visual token-strip" aria-hidden="true">
              <span style="--swatch: var(--lui-color-blue-3)">100</span>
              <span style="--swatch: var(--lui-color-blue-5)">300</span>
              <span style="--swatch: var(--lui-color-blue-6)">500</span>
              <span style="--swatch: var(--lui-color-blue-8)">700</span>
            </div>
          </div>

          <footer class="widget__footer">
            <a
              class="widget__cta"
              href="/case/locaweb-design-system/"
              data-link="locaweb-design-system"
            >
              <span data-i18n="locaweb-cta"></span>
              <svg width="14" height="14" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                <path
                  d="M3 8h9m0 0L8.5 4.5M12 8l-3.5 3.5"
                  stroke="currentColor"
                  stroke-width="1.4"
                  stroke-linecap="round"
                  stroke-linejoin="round"></path>
              </svg>
            </a>
          </footer>
        </article>

        <article
          class="widget widget--book"
          data-widget="book"
          tabindex="0"
          aria-roledescription="widget"
          aria-labelledby="widget-book-title"
        >

          <div class="widget__body">
            <img
              class="book-cover"
              src="/img/livro-design-system.jpg"
              width="336"
              height="480"
              loading="lazy"
              draggable="false"
              alt="Capa do livro Design System além do layout: Criação, estratégia e governança, de Mateus Villain"
            />

            <div>
              <lui-body
                variant="lg"
                as="h3"
                class="widget__title"
                id="widget-book-title"
              >
                <a
                  href="https://www.casadocodigo.com.br/products/livro-design-system"
                  target="_blank"
                  rel="noopener"
                  data-link="book"
                  data-i18n="book-title"></a>
              </lui-body>
              <lui-body variant="md" data-i18n="book-description"></lui-body>
            </div>
          </div>

          <footer class="widget__footer">
            <a
              class="widget__cta"
              href="https://www.casadocodigo.com.br/products/livro-design-system"
              target="_blank"
              rel="noopener"
              data-link="book"
            >
              <span data-i18n="book-cta"></span>
              <svg width="14" height="14" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                <path
                  d="M6 3h7v7M13 3 4 12"
                  stroke="currentColor"
                  stroke-width="1.4"
                  stroke-linecap="round"
                  stroke-linejoin="round"></path>
              </svg>
            </a>
          </footer>
        </article>

        <article
          class="widget widget--code"
          data-widget="letsui"
          tabindex="0"
          aria-roledescription="widget"
          aria-labelledby="widget-letsui-title"
        >

          <div class="widget__body">
            <lui-body
              variant="lg"
              as="h3"
              class="widget__title"
              id="widget-letsui-title"
            >
              <a
                href="https://github.com/mateusvillain/lets-ui-css/"
                target="_blank"
                rel="noopener"
                data-link="github-lets-ui"
                data-i18n="letsui-title"></a>
            </lui-body>
            <lui-body variant="md" data-i18n="letsui-description"></lui-body>

            <code class="widget__visual terminal">npm i @lets-ui/components</code>

          </div>

          <footer class="widget__footer">
            <a
              class="widget__cta"
              href="https://github.com/mateusvillain/lets-ui-css/"
              target="_blank"
              rel="noopener"
              data-link="github-lets-ui"
            >
              <span data-i18n="letsui-cta"></span>
              <svg width="14" height="14" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                <path
                  d="M6 3h7v7M13 3 4 12"
                  stroke="currentColor"
                  stroke-width="1.4"
                  stroke-linecap="round"
                  stroke-linejoin="round"></path>
              </svg>
            </a>
          </footer>
        </article>
      </div>
    </section>

    <section>
      <lui-heading variant="block-title" as="h2" data-i18n="blog-title"
      ></lui-heading>
      <ul class="no-list">
        <li>
          <a href="/blog/escalando-um-design-system-com-ia/">
            <article class="flex gap-fluid-16 interaction-item">
              <lui-body variant="lg" as="h3">
                Escalando um Design System com IA: Reduzindo trabalho e
                automatizando processos
              </lui-body>
              <time class="body--md space-nowrap">25 Mar 2026</time>
            </article>
          </a>
        </li>
        <li>
          <a href="/blog/design-no-figma-com-claude/">
            <article class="flex gap-fluid-16 interaction-item">
              <lui-body variant="lg" as="h3">
                Design no Figma com Claude: como usar IA para criar interfaces e
                controlar todo o seu Figma
              </lui-body>
              <time class="body--md space-nowrap">18 Mar 2026</time>
            </article>
          </a>
        </li>
        <li>
          <a href="/blog/design-tokens-resolver">
            <article class="flex gap-fluid-16 interaction-item">
              <lui-body variant="lg" as="h3">
                Design Tokens Resolver: o módulo nativo para gerar tokens e
                condicionais
              </lui-body>
              <time class="body--md space-nowrap">17 Fev 2026</time>
            </article>
          </a>
        </li>
      </ul>
    </section>
  </lui-stack>
</div>

<script>
  import '@lets-ui/components'

  /* ---------------------------------------------------------------------
     Widgets de projeto: arrastar com mouse, caneta e toque.
     - mouse: arrasta a partir de qualquer ponto do card (menos links)
     - toque/caneta: pressione e segure por um instante para "levantar" o
       card; assim o scroll vertical da pagina continua funcionando
  --------------------------------------------------------------------- */
  const board = document.getElementById('widget-board')
  const resetButton = document.getElementById('widgets-reset')

  if (board) {
    const widgets = Array.from(board.querySelectorAll('.widget'))
    const STORAGE_KEY = 'widget-positions'
    const DRAG_THRESHOLD = 5
    const HOLD_DELAY = 260
    const HOLD_TOLERANCE = 8
    const GUTTER = 8
    const STAGE_SLACK = 32
    const STEP = 12

    let topLayer = 1

    const readPositions = () => {
      try {
        return JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}')
      } catch {
        return {}
      }
    }

    const positions = readPositions()

    const savePositions = () => {
      try {
        localStorage.setItem(STORAGE_KEY, JSON.stringify(positions))
      } catch {
        /* modo privado: apenas nao persiste */
      }
      syncResetButton()
    }

    const syncResetButton = () => {
      if (!resetButton) return
      resetButton.hidden = !Object.keys(positions).length
    }

    const apply = (widget, x, y) => {
      widget.style.setProperty('--widget-x', `${Math.round(x)}px`)
      widget.style.setProperty('--widget-y', `${Math.round(y)}px`)
    }

    const clamp = (widget, x, y) => {
      const rect = widget.getBoundingClientRect()
      const stage = board.getBoundingClientRect()
      const current = positions[widget.dataset.widget] || { x: 0, y: 0 }
      // bordas do card sem o deslocamento atual
      const left = rect.left - current.x
      const right = rect.right - current.x
      const top = rect.top - current.y
      const bottom = rect.bottom - current.y
      // horizontal: dentro da janela, para nao criar rolagem lateral
      const minX = GUTTER - left
      const maxX = window.innerWidth - GUTTER - right
      // vertical: qualquer ponto do quadro, igual para os tres widgets
      const minY = stage.top - STAGE_SLACK - top
      const maxY = stage.bottom + STAGE_SLACK - bottom
      return {
        x: minX > maxX ? 0 : Math.min(Math.max(x, minX), maxX),
        y: minY > maxY ? 0 : Math.min(Math.max(y, minY), maxY),
      }
    }

    const move = (widget, x, y, persist) => {
      const id = widget.dataset.widget
      const next = clamp(widget, x, y)
      positions[id] = next
      apply(widget, next.x, next.y)
      if (persist) savePositions()
    }

    widgets.forEach((widget) => {
      const saved = positions[widget.dataset.widget]
      if (saved) apply(widget, saved.x, saved.y)

      let pointerId = null
      let startX = 0
      let startY = 0
      let originX = 0
      let originY = 0
      let armed = false
      let dragging = false
      let moved = false
      let holdTimer = null

      const arm = (event) => {
        armed = true
        moved = false
        pointerId = event.pointerId
        startX = event.clientX
        startY = event.clientY
        const current = positions[widget.dataset.widget] || { x: 0, y: 0 }
        originX = current.x
        originY = current.y
      }

      const lift = () => {
        dragging = true
        widget.style.zIndex = String(++topLayer)
        widget.classList.add('is-dragging')
        if (pointerId !== null) widget.setPointerCapture(pointerId)
      }

      widget.addEventListener('pointerdown', (event) => {
        if (event.pointerType === 'mouse' && event.button !== 0) return
        if (event.target.closest('a, button')) return

        arm(event)

        if (event.pointerType === 'mouse') {
          // no mouse o arrasto comeca assim que o cursor se move
          widget.setPointerCapture(event.pointerId)
          return
        }

        // toque e caneta: pressionar e segurar levanta o card
        holdTimer = window.setTimeout(() => {
          holdTimer = null
          if (armed) lift()
        }, HOLD_DELAY)
      })

      widget.addEventListener('pointermove', (event) => {
        if (!armed || event.pointerId !== pointerId) return
        const dx = event.clientX - startX
        const dy = event.clientY - startY
        const distance = Math.hypot(dx, dy)

        if (holdTimer !== null) {
          // rolou a pagina antes do tempo: nao era um arrasto
          if (distance > HOLD_TOLERANCE) {
            window.clearTimeout(holdTimer)
            holdTimer = null
            armed = false
          }
          return
        }

        if (!dragging) {
          if (distance < DRAG_THRESHOLD) return
          lift()
        }

        moved = true
        event.preventDefault()
        move(widget, originX + dx, originY + dy, false)
      })

      // impede o scroll nativo enquanto o card estiver levantado
      widget.addEventListener(
        'touchmove',
        (event) => {
          if (dragging) event.preventDefault()
        },
        { passive: false }
      )

      const endDrag = (event) => {
        if (event.pointerId !== pointerId) return
        if (holdTimer !== null) {
          window.clearTimeout(holdTimer)
          holdTimer = null
        }
        if (dragging) {
          widget.classList.remove('is-dragging')
          if (widget.hasPointerCapture(pointerId)) {
            widget.releasePointerCapture(pointerId)
          }
          if (moved) savePositions()
        }
        armed = false
        dragging = false
        pointerId = null
      }

      widget.addEventListener('pointerup', endDrag)
      widget.addEventListener('pointercancel', endDrag)

      // um arrasto nao deve disparar o link que estiver sob o cursor
      widget.addEventListener(
        'click',
        (event) => {
          if (!moved) return
          event.preventDefault()
          event.stopPropagation()
          moved = false
        },
        true
      )

      widget.addEventListener('dragstart', (event) => event.preventDefault())

      // teclado: setas movem o widget em foco, Escape devolve ao lugar
      widget.addEventListener('keydown', (event) => {
        const current = positions[widget.dataset.widget] || { x: 0, y: 0 }
        const step = event.shiftKey ? STEP * 4 : STEP

        if (event.key === 'Escape') {
          delete positions[widget.dataset.widget]
          apply(widget, 0, 0)
          savePositions()
          return
        }

        const deltas = {
          ArrowUp: [0, -step],
          ArrowDown: [0, step],
          ArrowLeft: [-step, 0],
          ArrowRight: [step, 0],
        }
        const delta = deltas[event.key]
        if (!delta) return

        event.preventDefault()
        widget.style.zIndex = String(++topLayer)
        move(widget, current.x + delta[0], current.y + delta[1], true)
      })
    })

    resetButton?.addEventListener('click', () => {
      widgets.forEach((widget) => {
        delete positions[widget.dataset.widget]
        apply(widget, 0, 0)
        widget.style.zIndex = ''
      })
      topLayer = 1
      savePositions()
    })

    // ao redimensionar, garante que nenhum widget fique fora da tela
    let resizeTimer
    window.addEventListener('resize', () => {
      window.clearTimeout(resizeTimer)
      resizeTimer = window.setTimeout(() => {
        widgets.forEach((widget) => {
          const current = positions[widget.dataset.widget]
          if (current) move(widget, current.x, current.y, false)
        })
      }, 150)
    })

    syncResetButton()
  }
</script>
